!!!
%html
  %head
  %body
    %h2 Sign in with Clef
    :ruby
      clef_options = {
        "data-app-id"       => settings.clef_app_id,
        "data-redirect-url" => "http://#{request.env["HTTP_HOST"]}/login",
        "data-state"        => @state,
      }

    - if logged_in?
      %h4
        Logged in as
        = current_user.fetch :id

      - if current_user[:email]
        %p email: #{current_user[:email]}

      %a{ href: "/logout" } Logout

    - else
      %script.clef-button{ { src: "https://clef.io/v3/clef.js", type: "text/javascript" }.merge(clef_options) }
